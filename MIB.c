#include<stdio.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int n,i;
        scanf("%d",&n);
        char a[n][102];
        int priority[n];
        for(i=0;i<n;i++)
            scanf("%s",a[i]);
        //printf("%s",a[0]);
            }
}
